- hosts: local
  tasks:
    - name: Create directory
      file:
        path: /tmp/hpc_ansible
        state: directory

    - name: Write status file
      copy:
        content: "HPC environment configured"
        dest: /tmp/hpc_ansible/status.txt
