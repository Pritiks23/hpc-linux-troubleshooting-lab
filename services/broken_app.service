[Unit]
Description=Intentionally broken app service
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/nonexistent-app --serve
Restart=on-failure

[Install]
WantedBy=multi-user.target
