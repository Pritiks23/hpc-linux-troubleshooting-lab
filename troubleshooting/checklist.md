# Incident Checklist

1. Check service status
2. Inspect logs (journalctl)
3. Check CPU/memory (top, free -h)
4. Check disk (df -h)
5. Check network ports (ss -tupln)
6. Validate config files
7. Check permissions (ls -l, chown, chmod)
